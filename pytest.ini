[pytest]
minversion = 6.0
addopts = -ra -q --asyncio-mode=auto
asyncio_default_fixture_loop_scope = function
testpaths =
    tests
python_files = test_*.py
# Filter out warnings from dependencies if needed
# filterwarnings =
#     ignore::DeprecationWarning
